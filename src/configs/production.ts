const config: AppConfig = {
    backendUrl: process.env.BACKEND_URL || 'https://backend.stg.hive-store.ru',
    frontendUrl: process.env.FRONTEND_URL || 'https://frontend.stg.hive-store.ru'
}

export default config